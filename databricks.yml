# This is a Databricks asset bundle definition for gitaction_dabs_cicd.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: gitaction_dabs_cicd
  uuid: 3464100a-5b2c-4e22-afb2-9f12aca3669e

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2702709954875638.18.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-2702709954875638.18.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/samanarika52@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/samanarika52@outlook.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: samanarika52@outlook.com
        level: CAN_MANAGE
